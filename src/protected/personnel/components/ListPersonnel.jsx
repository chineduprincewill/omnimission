import React from 'react'

const ListPersonnel = ({ title }) => {
    return (
        <div className='w-full m-0'>
            <h1 className='text-lg capitalize'>{title}</h1>
        </div>
    )
}

export default ListPersonnel